{
  "private": true,
  "name": "tests",
  "author": "You <you@example.com>",
  "version": "0.1.0",
  "scripts": {
    "test:foo": "cd src/foo && yarn install",
    "test": "yarn test:foo && yarn install && rimraf dist/js && rollup --config",
    "serve": "live-server dist"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^22.0.0",
    "@rollup/plugin-node-resolve": "^13.2.1",
    "@wasm-tool/rollup-plugin-rust": "file:..",
    "live-server": "^1.2.1",
    "rimraf": "^3.0.2",
    "rollup": "^2.0.0"
  }
}
